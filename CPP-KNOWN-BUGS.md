outer-edge-1x1 should just begin by filling the outer edge with nothing but 1x1s and then try to fill the remaining inner area with more tiles. At least, this should be true in MQT mode.

Currently it just tries to generate tiles that have 1x1 as their outer edge and which are placed beginning in the upper-left corner